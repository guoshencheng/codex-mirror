server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    location = / { return 302 /display/; }
    location /display/ {
        try_files $uri $uri/ /display/index.html;
        add_header Cache-Control "no-cache" always;
    }
}
